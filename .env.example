# Copy to .env and fill in. The chat proxy reads these from the environment.
# .env is gitignored — never commit your real key.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE=https://api.openai.com/v1
PORT=8000
